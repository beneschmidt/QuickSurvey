here we go:
mit git bash in verzeichnis gehen und
"npm install -g grunt-cli" ausführen
"npm install -g bower" ausführen
"npm install pg" ausführen
"npm install basic-logger" ausführen
"npm install" ausführen
"bower install" ausführen (falls das nicht geht dann "bower install jquery" & "bower install d3")
"grunt serve" ausführen --> sollte deinen browser starten und die client anwendung öffnen
-- NOT YET: "node src/server/server.js" sollte den server-teil der anwendung starten