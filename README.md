## Entwicklung mit Grunt
Node.js muss installiert sein.
### Setup
Zur Installation müssen zunächst folgende Befehle ausgeführt werden:
```
npm install
npm install basic-logger
npm install pg
```
### Run Server
```
grunt server
```
